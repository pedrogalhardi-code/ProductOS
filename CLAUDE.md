# ProductOS

Personal/exploratory build — full-stack scaffold (`client/`, `server/`, `shared/`).

See `README.md` for stack, setup, and usage. Local services run via `docker-compose.yml`; `start.sh` boots the dev environment.
